version: "3.9"

services: 
  redis:
    image: redis:latest
    container_name: redis_container
    ports:
      - "6379:6379"
    volumes:
      - ./redisdata:/data
    command: redis-server --save 20 1 --loglevel warning --requirepass password1
